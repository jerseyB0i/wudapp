// TODO: hold-to-record, waveform animation, swipe-to-cancel
export function VoiceNoteRecorder({
	onDone: _onDone
}: {
	onDone: (blob: Blob, durationMs: number) => void;
}) {
	return <div>VoiceNoteRecorder</div>;
}
